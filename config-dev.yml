server:
  port: 3355

spring:
  application:
    name: config-client
  cloud:
    config:
      label: main
      name: config
      profile: dev
      uri: http://localhost:3344

#eureka客户端
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka

config:
  info: configInfo测试数据